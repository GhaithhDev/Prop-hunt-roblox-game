local GroupRewardsConfig = {}

-- PLACEHOLDER GroupId (0) - replace with the real Roblox Group ID.
-- GroupRewardsService.Start() warns loudly while this is still 0.
GroupRewardsConfig.GroupId = 0

GroupRewardsConfig.XpReward = 15000

return GroupRewardsConfig
