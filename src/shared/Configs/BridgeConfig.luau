return {
	CountdownDuration = 2, -- seconds - matches the Timer's seconds:hundredths display
	FadeDuration = 1, -- ReplicatedTween fade-out length
	GoneDuration = 2, -- seconds spent invisible + non-collidable before restoring
}
